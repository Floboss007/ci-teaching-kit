# --- Terraform ---
terraform/*.tfstate
terraform/*.tfstate.*
terraform/*.tfstate.backup
terraform/.terraform/
terraform/terraform.tfvars
terraform/*.tfplan
terraform/crash.log
terraform/crash.*.log

# --- GCP service account key — never commit this ---
github-actions-key.json

# --- Maven build output ---
app/target/

# --- OS-specific junk files ---
.DS_Store
Thumbs.db
